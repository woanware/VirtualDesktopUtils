<Window
  x:Class="VirtualDesktopUtils.WindowTrayMenu"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  Title="TrayMenu"
  WindowStyle="None"
  AllowsTransparency="True"
  Background="Transparent"
  SizeToContent="WidthAndHeight"
  Topmost="True"
  ShowInTaskbar="False"
  ResizeMode="NoResize"
  Deactivated="Window_Deactivated"
  PreviewKeyDown="Window_PreviewKeyDown"
>
  <Window.Resources>
    <Style x:Key="TrayMenuItem" TargetType="Button">
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Padding" Value="14,8" />
      <Setter Property="HorizontalContentAlignment" Value="Left" />
      <Setter Property="FontSize" Value="13" />
      <Setter Property="FontFamily" Value="Segoe UI" />
      <Setter Property="Foreground" Value="#E0E0E0" />
      <Setter Property="FocusVisualStyle" Value="{x:Null}" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border
              x:Name="Bd"
              Background="Transparent"
              CornerRadius="6"
              Padding="{TemplateBinding Padding}"
            >
              <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="#3A3A3A" />
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="#454545" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Window.Resources>

  <Border Padding="8">
    <Border
      CornerRadius="12"
      MinWidth="180"
    >
      <Border.Background>
        <SolidColorBrush Color="#2C2C2C" />
      </Border.Background>
      <Border.Effect>
        <DropShadowEffect BlurRadius="20" ShadowDepth="2" Opacity="0.5" Color="Black" />
      </Border.Effect>
      <Border
        CornerRadius="12"
        BorderThickness="1"
        BorderBrush="#40FFFFFF"
        Padding="6"
      >
        <StackPanel>
          <Button Content="Show" Click="Show_Click" Style="{StaticResource TrayMenuItem}" />
          <Button Content="Refresh" Click="Refresh_Click" Style="{StaticResource TrayMenuItem}" />
          <Border Height="1" Background="#30FFFFFF" Margin="6,4" CornerRadius="1" />
          <Button Content="Exit" Click="Exit_Click" Style="{StaticResource TrayMenuItem}" />
        </StackPanel>
      </Border>
    </Border>
  </Border>
</Window>
